---
phase: 25-build-system-integration
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - Makefile
autonomous: true

must_haves:
  truths:
    - "User runs `make` and both abyss.exe and mcluster_sse are built"
    - "User without gfortran sees warning and ABYSS still builds"
    - "User runs `make clean` and mcluster artifacts are removed"
    - "User runs `make mcluster` and only mcluster builds"
    - "User runs `make mcluster-rebuild` and mcluster is rebuilt from scratch"
  artifacts:
    - path: "Makefile"
      provides: "Top-level build orchestration"
      min_lines: 50
      contains: "BUILD_MCLUSTER"
    - path: "src/mcluster"
      provides: "Symlink to mcluster binary"
      type: "symlink"
  key_links:
    - from: "Makefile"
      to: "src/Makefile"
      via: "$(MAKE) -C src"
      pattern: "\\$\\(MAKE\\).*-C src"
    - from: "Makefile"
      to: "mcluster/Makefile"
      via: "$(MAKE) -C mcluster"
      pattern: "\\$\\(MAKE\\).*-C mcluster"
---

<objective>
Create top-level Makefile that orchestrates ABYSS and McLuster builds

Purpose: Enable users to build entire project with single `make` command, with graceful handling when Fortran compiler is unavailable

Output: Root-level Makefile with gfortran detection, conditional mcluster build, and appropriate targets
</objective>

<execution_context>
@~/.claude/get-shit-done/workflows/execute-plan.md
@~/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/25-build-system-integration/25-CONTEXT.md
@.planning/phases/25-build-system-integration/25-RESEARCH.md
@src/Makefile
@mcluster/Makefile
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create root-level Makefile with mcluster integration</name>
  <files>Makefile</files>
  <action>
Create a new root-level Makefile at `/gpfs/home/vjl4366/pkg/ABYSS/Makefile` that orchestrates both ABYSS and McLuster builds.

Requirements from CONTEXT.md decisions:
1. **Compiler detection**: Use `$(shell which gfortran 2>/dev/null)` for immediate evaluation
2. **Conditional build**: Skip mcluster with warning if gfortran unavailable (not hard error)
3. **DISABLE_MCLUSTER flag**: Allow explicit skip even when gfortran available
4. **Default target**: `all` builds ABYSS + mcluster (if available)
5. **Explicit targets**: `mcluster`, `mcluster-clean`, `mcluster-rebuild`
6. **Clean integration**: `make clean` removes both ABYSS and mcluster artifacts
7. **Symlink**: Create `src/mcluster` symlink pointing to `../mcluster/mcluster_sse`
8. **Success message**: Print "McLuster built successfully" on completion
9. **Quiet mode**: Support `QUIET=1` to suppress output
10. **Build failure**: If mcluster build fails, stop entire build (no `-` prefix)

Use patterns from RESEARCH.md:
- Use `:=` for shell command (immediate evaluation)
- Use `$(MAKE)` not bare `make` (preserves -j flags)
- Use `$(MAKE_QUIET)` with `--no-print-directory` in quiet mode
- Use `ifeq` for conditional targets
- Mark all targets as `.PHONY`

Structure:
```
# Header comment explaining usage
# Environment variable documentation (DISABLE_MCLUSTER, QUIET)
# gfortran detection
# BUILD_MCLUSTER determination
# Quiet mode setup
# Default target (conditional on BUILD_MCLUSTER)
# abyss target
# mcluster target (conditional implementation)
# mcluster-clean target
# mcluster-rebuild target
# clean target (calls both)
# .PHONY declaration
```
  </action>
  <verify>
File exists: `ls -la /gpfs/home/vjl4366/pkg/ABYSS/Makefile`
Contains key patterns: `grep -E "(BUILD_MCLUSTER|GFORTRAN|MAKE.*-C)" /gpfs/home/vjl4366/pkg/ABYSS/Makefile`
  </verify>
  <done>Root Makefile exists with gfortran detection, conditional mcluster build, all required targets, and symlink creation</done>
</task>

<task type="auto">
  <name>Task 2: Verify build system functionality</name>
  <files>Makefile, src/mcluster</files>
  <action>
Test the build system with multiple scenarios to ensure all requirements are met.

Test sequence:
1. **Full build test**: Run `make` from project root and verify both binaries are built
   - Check abyss.exe exists: `ls -la src/abyss.exe`
   - Check mcluster_sse exists: `ls -la mcluster/mcluster_sse`
   - Check symlink exists: `ls -la src/mcluster`

2. **Clean test**: Run `make clean` and verify all artifacts removed
   - Check abyss.exe removed
   - Check mcluster artifacts removed
   - Check symlink removed

3. **Mcluster-only test**: Run `make mcluster` and verify only mcluster built
   - Check mcluster_sse exists
   - Check symlink exists

4. **Mcluster-clean test**: Run `make mcluster-clean` and verify mcluster artifacts removed
   - Check mcluster_sse removed
   - Check symlink removed
   - Check ABYSS .o files untouched (if any exist)

5. **Mcluster-rebuild test**: Run `make mcluster-rebuild` and verify full rebuild
   - Check mcluster_sse exists after rebuild

6. **DISABLE_MCLUSTER test**: Run `DISABLE_MCLUSTER=1 make` and verify mcluster skipped
   - Check abyss.exe built
   - Check mcluster NOT attempted

7. **Quiet mode test**: Run `QUIET=1 make mcluster` and verify reduced output

8. **Parallel build test**: Run `make -j4` and verify no race conditions
   - Both targets should complete successfully

If any test fails, diagnose the issue and fix the Makefile.
  </action>
  <verify>
All tests pass:
- `make` builds both binaries
- `make clean` removes all artifacts
- `make mcluster` builds only mcluster
- `make mcluster-clean` removes mcluster artifacts
- `make mcluster-rebuild` rebuilds mcluster
- `DISABLE_MCLUSTER=1 make` skips mcluster
- Symlink `src/mcluster` points to correct location
  </verify>
  <done>
BUILD-01 verified: McLuster compiles with gfortran + gcc (mcluster_sse binary exists)
BUILD-02 verified: mcluster_sse produced alongside abyss.exe (both in src/ via symlink)
BUILD-03 verified: gfortran detection works (warning printed if missing, DISABLE flag works)
BUILD-04 verified: Clean/rebuild targets function correctly
  </done>
</task>

</tasks>

<verification>
Phase 25 requirements verification:

| Requirement | Verification Command | Expected Result |
|-------------|---------------------|-----------------|
| BUILD-01 | `ls mcluster/mcluster_sse` | File exists after `make` |
| BUILD-02 | `ls src/mcluster` | Symlink exists pointing to mcluster_sse |
| BUILD-03 | `DISABLE_MCLUSTER=1 make 2>&1 \| head -5` | Builds ABYSS without mcluster |
| BUILD-04 | `make mcluster-clean && ls mcluster/*.o 2>&1` | No .o files remain |
</verification>

<success_criteria>
1. `make` from project root builds both abyss.exe and mcluster_sse
2. `src/mcluster` symlink exists and points to `../mcluster/mcluster_sse`
3. User without gfortran gets warning message (not cryptic error)
4. `make clean` removes all build artifacts including mcluster
5. `make mcluster`, `make mcluster-clean`, `make mcluster-rebuild` all work independently
6. `DISABLE_MCLUSTER=1` flag skips mcluster build
7. Parallel build (`make -j4`) works without race conditions
</success_criteria>

<output>
After completion, create `.planning/phases/25-build-system-integration/25-01-SUMMARY.md`
</output>
